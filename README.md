# Book library on python + postgresql

## To run:

- clone repo and go to project directory
- create venv for project (python -m venv venv) and run venv(. ./venv/bin/activate)
- install requirements(pip install -r requirements.txt)
- create database(postgresql) with name booksdb for user postgres
- to run app(python book_lib.py)
